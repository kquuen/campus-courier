NEW_FILE_CODE
# SLA Performance Targets
# P95 latency < 200ms for core APIs
sla.p95.latency.ms=200
sla.p99.latency.ms=500

# Core APIs to test
core.apis=/api/user/login,/api/order/pending,/api/order/publish,/api/payment/pay,/api/order/{id}/accept

# Concurrent users for load test
load.test.concurrent.users=50
load.test.duration.seconds=60
load.test.ramp.up.seconds=10
